package com.firepitmedia.earnit.utils.Repository;

import android.content.Context;

public class RestRepository extends Repository implements RestRepositoryContract{

    public RestRepository(Context mContext) {
        super(mContext);
    }
}
